package keretrendszer.Repositories;

import keretrendszer.Models.Psu;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PsuRepository extends JpaRepository<Psu, Long> {
}
